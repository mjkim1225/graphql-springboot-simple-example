server:
  port: 8070
  shutdown: graceful

spring:
  datasource:
    url:  jdbc:h2:~/test
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql: true
  sql:
    init:
      mode: always
  graphql:
    graphiql:
      enable: true # graphiql? true? ??? ?? localhost:8080/h2-console? ?? localhost:8080/graphiql ?? graphql ??? ???? ?????.
                   # ? ?? ??? IntelliJ? GraphQL ????? ???? IntelliJ?? ?? ???? ???? Postman??? ?????.
      printer:
        enable: true # ? ?? ? jpa? show-sql ?? graphql ??? ??? ???.

